/**
 * Configurazione principale di Next.js.
 * Permette di personalizzare il comportamento del framework, come l'instradamento,
 * la gestione delle immagini, le variabili d'ambiente e le ottimizzazioni di build.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
